#################
### MONITORS  ###
#################

# En QEMU/KVM con virtio-gpu, el monitor suele llamarse Virtual-1
# Forzamos la resolución nativa y desactivamos el escalado para ahorrar CPU.
monitor = Virtual-1, 1920x1080@60, 0x0, 1
# Fallback por si cambia el nombre
monitor = , preferred, auto, 1
